{% for node in pieces_list %}
{% if group == null or group == node.group %}
<div>
  <div style="float:left">
    <h3> {{node.piece}} | {{node.duration}} </h3>
  </div>
  <div style="float:right">
    <h3>{{node.year}}</h3>
  </div>
</div>
{{node.recording}}
<div style="text-align:right">
  <!-- TODO add parts -->
  <a href="{{node.score}}">Score</a>
</div>
<div style="text-align:justified">
{{node.notes}}
</div>
  {% endif %}
{% endfor %}
{% assign pieces_list = nil %}
{% assign group = nil %}
